# Nikita Chaurasia — Portfolio

Personal portfolio built with **Next.js 14**, **TypeScript**, **Tailwind CSS**, and **Framer Motion**.

## Run locally

Node.js 18+ required.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Build for production

```bash
npm run build
```

Static export output is written to `out/` (configured for GitHub Pages / Vercel).

## Deploy

- **Vercel:** import repo and deploy (zero config)
- **GitHub Pages:** run `npm run build` and publish the `out/` folder

## Assets

- Profile photo: `public/nikita-chaurasia.jpg`
- Resume PDF: `public/Nikita Chaurasia Resume.pdf`

## TODO

- Convert hero photo to WebP/AVIF after build tooling is available for smaller payload
- Photo background includes event lanyard — consider a tighter crop if you prefer a cleaner hero
